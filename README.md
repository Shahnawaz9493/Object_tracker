# Object_tracker
Real-time multi-object detection and tracking using YOLOv8 + SORT (Kalman Filter + Hungarian Algorithm). Features persistent track IDs, motion trails, live HUD, and a zero-crash multi-backend detector. Built with OpenCV and Python.
